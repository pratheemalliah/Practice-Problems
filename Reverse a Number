function reverse(n) {
  
  let reverse = 0;
  
  while(n > 0) {
    let digit = n%10;
    reverse = reverse * 10 + digit;
    console.log(reverse)
    n = Math.floor(n/10);
  }
  
  return reverse;
}


console.log(reverse(2999864))
